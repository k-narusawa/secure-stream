spring:
  profiles:
    active: local

server:
  servlet:
    session:
      cookie:
        secure: true
        http-only: true

env:
  cors:
    allowed-origin: ${ALLOWED_ORIGIN}
  hydra:
    admin:
      url: ${HYDRA_ADMIN_CLIENT:http://localhost:4445}